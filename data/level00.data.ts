export const level00Name = 'Level 0: First Steps';

export const level00Objective = 'Objective: Move the rover () to the goal ().';

export const level00Start = `// The code below moves the rover, but it's not going to the
// right place. Try changing the code to see what happens.

// CHANGE THE CODE BELOW
move_forward(3);
turn_right();
move_forward(1);`;

export const level00Solution = `// The code below moves the rover, but it's not going to the
// right place. Try changing the code to see what happens.

// CHANGE THE CODE BELOW
move_forward(3);
turn_right();
move_forward(3);`;
