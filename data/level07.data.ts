export const level07Start = `// Looks like there's a crate blocking the way. Use the pick_up
// and drop functions to move it.`;

export const level07Solution = `// Looks like there's a crate blocking the way. Use the pick_up
// and drop functions to move it.
move_forward(1);
pick_up();
turn_right();
turn_right();
drop();
move_backward(3);`;
