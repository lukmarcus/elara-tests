export const level10Start = `// First, we move G.R.O.V.E.R. next to the password gate.
move_forward(2);

// Then, you just need to say the password.
// Reminder: the password for this gate is "lovelace".
// ADD YOUR CODE BELOW`;

export const level10Solution = `// First, we move G.R.O.V.E.R. next to the password gate.
move_forward(2);

// Then, you just need to say the password.
// Reminder: the password for this gate is "lovelace".
// ADD YOUR CODE BELOW
say("lovelace");
move_forward(5);`;
