export const level12Start = `// This code reads the password from the data point and
// stores it in a variable called password. (You DON'T
// need to change this part).
move_forward(1);
let password = read_data();

// Now you just need to unlock the gate and move to the goal.
// ADD YOUR CODE BELOW`;

export const level12Solution = `// This code reads the password from the data point and
// stores it in a variable called password. (You DON'T
// need to change this part).
move_forward(1);
let password = read_data();

// Now you just need to unlock the gate and move to the goal.
// ADD YOUR CODE BELOW
move_forward(1);
say(password);
move_forward(3);`;
